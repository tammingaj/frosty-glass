'use strict';

describe('frostyGlass.library module', function() {

	beforeEach(module('frostyGlass.library'));

	describe('library controller', function(){

		it('should ....', inject(function($controller) {
			//spec body
			var libraryCtrl = $controller('LibraryCtrl');
			expect(libraryCtrl).toBeDefined();
		}));

	});
});
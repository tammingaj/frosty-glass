'use strict';

describe('frostyGlass.reader module', function() {

	beforeEach(module('frostyGlass.reader'));

	describe('reader controller', function(){

		it('should ....', inject(function($controller) {
			//spec body
			var readerCtrl = $controller('ReaderCtrl');
			expect(readerCtrl).toBeDefined();
		}));

	});
});